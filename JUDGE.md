# 채점기 (`judge.py`)

[`judging_rubric.md`](judging_rubric.md) 그대로 제출물을 채점하는 도구. 제출 전에 우리 팀
점수를 미리 확인하는 용도다. 로컬에 설치된 CLI(`claude` / `codex` / `grok`)를 심사관
백엔드로 쓴다. 파이썬 3.10+ 외 의존성 없음.

```bash
python judge.py sample_submission                  # 3페르소나 x 3라운드 = 9회 채점
python judge.py <제출물_디렉터리> --url https://우리서비스.app   # URL 접속 여부까지 확인
python judge.py <제출물_디렉터리> --backends grok --rounds 1   # 빠른 확인
python judge.py --selftest                         # 집계 수학 + 루브릭 대조 검증
```

> **이건 리허설이지 성적표가 아니다.** 주최측 채점기는 서비스 URL 에 직접 접속해
> 3단계를 따라가지만, 이 채점기는 `05_evidence.md` 에 적힌 검증 결과를 그대로 믿는다.
> 절대 점수 대신 리포트의 **먼저 고칠 것**과 **걸린 상한**을 봐라 — 그건 주최측
> 채점기에서도 똑같이 걸린다.

### 제출물 형식

디렉터리 안의 `.md` / `.txt` / `.json` / `.html` 파일을 전부 읽어 하나로 합쳐 심사관에게
넘긴다. 파일 이름은 자유지만, 실제 제출 폼의 필드를 그대로 옮겨 담으면 된다.
`sample_submission/` 이 그 예시다 — 복사해서 내용만 바꿔 쓴다.

| 파일 | 제출 폼 필드 | 채점에 쓰이는 곳 |
| :--- | :--- | :--- |
| `01_basic.md` | 팀 이름, 서비스 URL, 테스트 계정, **핵심 플로우 3단계** | A1, A3 |
| `02_plan.md` | 기획안 (최대 8,000자) | A3, A4, 게이트 |
| `03_script.md` | 발표 스크립트 (최대 4,000자) | B 전체, C 전체 |
| `04_slides.html` | 발표자료 HTML (선택) | B, C 보강 |
| `05_evidence.md` | 브라우저 검증 결과 | A1, A2, B·C 구현 대조 |

발표자료 HTML 은 태그를 걷어내고 글자만 뽑는다(`<script>` · `<style>` 내용 제외).
실제 심사도 "글자 내용만" 쓴다고 안내하므로 같은 조건이 된다.

`05_evidence.md` 는 심사 에이전트가 서비스 URL 에 접속해 핵심 플로우 3단계를 따라간 결과에
해당한다. 우리가 직접 브라우저로 돌려보고 단계별 성공·실패와 콘솔 에러를 적어 넣으면 된다.
이게 없으면 A1·A2 를 채점할 근거가 없고, B·C 의 기능 주장도 "구현 미확인"으로 깎인다.

### 출력

`judging_result/report.md`

- **먼저 고칠 것** — 고쳤을 때 총점이 가장 많이 오르는 순서(= 총점 기여도 x 남은 점수).
  항목마다 걸린 상한, 루브릭이 요구하는 조건 문장, 심사관이 실제로 인용한 원문을 같이 보여준다.
  인용이 비어 있으면 그 항목의 근거가 제출물에 없다는 뜻이다.
- 항목별 점수 표 — 페르소나별 중앙값, 총점 기여도, **점수 범위**, 걸린 상한.
  범위가 넓은 항목은 심사관마다 읽는 방식이 갈린다는 뜻이라 문서가 애매한 것이다.
- 제출물 점검 경고 — 서비스 URL 누락, 검증 결과 누락, 3단계 누락, 검증 결과에 실패가
  하나도 없는 경우(자기보고 편향).

`judging_result/raw.json` — 9회 채점 원본(각 항목의 근거 인용과 이유 포함).

### 동작 방식

- 루브릭 **문장**은 `judging_rubric.md` 를 런타임에 읽어 프롬프트에 통째로 넣는다.
  코드에는 **숫자**(섹션·항목 가중치, 상한, 게이트 배율)만 있다. 루브릭을 고치면 채점도 따라 바뀐다.
- 상한 규칙은 모델에 맡기지 않는다. 심사관은 `cap_met` / `intent_only` / `impl_check` /
  `c2_external_only` 플래그만 채우고, 실제 감점은 `apply_caps()` 가 결정적으로 적용한다.
  루브릭 공통 규칙 4개(항목별 상한, 의도만이면 ≤5, 구현 대조 ≤6/≤4, 인용 근거 없으면 <7)가
  전부 코드로 강제된다.
- 인용문은 제출물 원문과 대조한다. 모델이 요약·재작성한 인용은 근거로 인정하지 않으므로
  확인 불가능한 인용으로 상한을 넘길 수 없다.
- 집계: 페르소나별 3회 **중앙값** → 페르소나 **평균** → 섹션 가중합 → 게이트 배율.
- 라운드마다 백엔드를 바꿔 돌리므로 한 모델의 편향을 중앙값이 깎는다.
- 9회를 병렬로 호출하고, 일부가 실패해도 나머지로 집계한 뒤 실패 목록과 경고를 리포트에 남긴다.
- 제출물은 데이터로 취급한다. 제출물 안에 심사관을 향한 지시문이 있어도 따르지 않는다.
- 코드의 수치(가중치·상한·게이트 배율·항목 이름)를 매 실행마다 `judging_rubric.md` 와
  대조해서, 어긋나면 채점하지 않고 멈춘다. 루브릭이 갱신됐는데 코드가 그대로인 상황을
  조용히 넘기지 않기 위한 것.

### 옵션

| 옵션 | 기본값 | 설명 |
| :--- | :--- | :--- |
| `--backends` | `claude,codex,grok` | 쓸 CLI. 설치된 것만 지정한다 |
| `--rounds` | `3` | 페르소나당 채점 횟수 |
| `--timeout` | `900` | CLI 호출 하나당 초 |
| `--max-chars` | `40000` | 제출물 텍스트 상한. 한 파일이 절반을 넘지 못하고, 발표자료가 뒤로 밀린다 |
| `--url` | 없음 | 주면 서비스 URL 이 밖에서 열리는지 확인한다. 접속 여부만 — 3단계·콘솔 에러는 사람이 봐야 한다 |
| `--out` | `judging_result` | 결과 디렉터리 |
